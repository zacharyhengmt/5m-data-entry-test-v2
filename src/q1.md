# Question 1 — Multiple Choice: Core Concepts

**Format:** Multiple Choice Questions (MCQ, 15 parts across Python, CLI, Git, and Operating Systems)

---

Read the code block below and answer the questions that follow. Note: not all lines may be valid Python.

```python
MAX_RETRIES = 5
user_name = "Ada"
1st_value = 10
PI = 3.14159
```

---

**1.** Which of the following observations about the code above is correct?

- A. All four lines are valid Python variable declarations.
- B. `MAX_RETRIES` and `PI` follow the Python convention for constants, but `1st_value` is not a valid identifier.
- C. `user_name` is not a valid Python identifier because it contains an underscore.
- D. `1st_value` is valid because Python allows identifiers that start with digits.

**Answer:** B

---

**2.** In Python, constants are:

- A. Enforced by the interpreter, which raises an error if you try to reassign them.
- B. A convention only. Python does not prevent reassignment of ALL_CAPS names.
- C. Defined using the `const` keyword.
- D. Only allowed inside functions.

**Answer:** B

---

**3.** Which variable name below follows Python's PEP 8 style guide for a regular variable?

- A. `MyUserAge`
- B. `my-user-age`
- C. `my_user_age`
- D. `MYUSERAGE`

**Answer:** C

**4.** Which command installs the `pandas` package into the currently active conda environment?

- A. `conda install pandas`
- B. `pip install pandas --global`
- C. `conda add pandas`
- D. `install pandas`

**Answer:** A

---

**5.** A `.env` file is commonly used in Python projects to:

- A. Store encrypted passwords in compiled bytecode.
- B. Define environment variables such as API keys that should not be hard-coded in source files.
- C. Replace the `requirements.txt` file for managing package versions.
- D. Automatically activate a conda environment when the script runs.

**Answer:** B

---

**6.** You have a file called `environment.yml`. Which command creates a conda environment from it?

- A. `conda create environment.yml`
- B. `conda env create -f environment.yml`
- C. `conda activate environment.yml`
- D. `conda load -f environment.yml`

**Answer:** B

---

> **Note:** For questions that reference file paths (e.g. `/home/user/projects`), assume you are working in a Linux or macOS terminal, or in WSL on Windows.

**7.** Which command lists the files and folders in the current directory?

- A. `pwd`
- B. `ls` (or `dir` on Windows Command Prompt)
- C. `cd`
- D. `cat`

**Answer:** C

---

**8.** You are in the directory `/home/user/projects`. Which command moves you up one level to `/home/user`?

- A. `cd /home/user/projects/..`
- B. `cd ..`
- C. `cd -`
- D. `cd /`

**Answer:** B

---

**9.** Which command creates a new directory called `data` inside the current directory?

- A. `touch data`
- B. `new data`
- C. `mkdir data`
- D. `create dir data`

**Answer:** C

---

**10.** What does the `pwd` command do?

- A. Prints the contents of a file.
- B. Displays the full path of the current working directory.
- C. Changes the current directory to the home folder.
- D. Deletes the current directory.

**Answer:** B

---

**11.** What is the difference between `git clone` and forking a repository?

- A. They are the same: both create an independent copy on GitHub under your account.
- B. `git clone` downloads a local copy of a repository; forking creates your own copy of the repository on GitHub.
- C. Forking only works for private repositories; `git clone` works for public ones.
- D. `git clone` requires you to be a repository collaborator; forking does not.

**Answer:** B

---

**12.** Which command would you run to download a copy of the repository at `https://github.com/su-ntu-ctp/5m-data-entry-test.git` to your local machine?

- A. `git pull https://github.com/su-ntu-ctp/5m-data-entry-test.git`
- B. `git fetch https://github.com/su-ntu-ctp/5m-data-entry-test.git`
- C. `git clone https://github.com/su-ntu-ctp/5m-data-entry-test.git`
- D. `git download https://github.com/su-ntu-ctp/5m-data-entry-test.git`

**Answer:** C

---

**13.** What does `git status` show you?

- A. A list of all commits made on the current branch.
- B. The differences between your local branch and the remote branch.
- C. The state of the working directory and staging area, including which files are tracked, modified, staged, or untracked.
- D. The names of all branches in the repository.

**Answer:** C

---

**14.** What is a kernel?

- A. A Python library for numerical computation.
- B. The core component of an operating system that manages hardware resources and allows software to interact with the CPU, memory, and devices.
- C. A type of virtual machine that runs inside a container.
- D. The graphical user interface (GUI) layer of an operating system.

**Answer:** B

---

**15.** In the context of terminals and shells, which statement is CORRECT?

- A. A terminal and a shell are the same program.
- B. A terminal is the interface (window) through which you interact with a shell; the shell is the program that interprets and executes your commands (e.g., bash, zsh, PowerShell).
- C. PowerShell is a Linux-only shell.
- D. Bash is the default shell on Windows 11.

**Answer:** B
